en:
  spotlight:
    resources:
        dri_harvester:
            form:
              title: 'DRI Repository'
              url: "Base URL"
              add_item: "Add items"
              url-field:
                help: "Add the base URL of the DRI Repository."
              user: "User"
              user-field:
                help: "User email."
              token-field:
                help: "User token."
              ids-field:
                help: "List of DRI object IDS."
              metadata-field:
                help: "List of metadata fields to retrieve, leave blank for default set."
            performharvest:
              success: "Successfully retrieved DRI objects."
              failed: "Unable to retrieve DRI objects."
